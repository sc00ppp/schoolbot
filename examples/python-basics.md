# Example: learning Python loops

This is fictional sample material. Copy this file into your course's `notes/`
folder to try SchoolBot before adding real coursework.

## What I know

A list holds multiple values. A `for` loop visits each value in order.

```python
scores = [7, 9, 8]
total = 0
for score in scores:
    total += score
print(total)
```

## What I want to practice

- Explain why `total = 0` goes before the loop.
- Predict the output before running the program.
- Change the example to calculate the average.

## Question for my next study session

How is returning a value from a function different from printing it?
